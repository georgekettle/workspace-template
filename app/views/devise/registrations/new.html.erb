<h2 class="h2">Sign up</h2>

<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render 'shared/error_messages', form: f %>

  <div class="form-inputs">
    <div class="grid md:grid-cols-2 gap-5">
      <%= f.hidden_field(:invitation_token, value: params[:invitation_token], name: :invitation_token) if params[:invitation_token] %>
      <%= f.input :first_name,
                  required: true,
                  autofocus: true,
                  placeholder: t('placeholder.first_name'),
                  input_html: { autocomplete: "first_name" }%>
      <%= f.input :last_name,
                  required: true,
                  placeholder: t('placeholder.last_name'),
                  input_html: { autocomplete: "last_name" }%>
    </div>
    <%= f.input :email,
                required: true,
                placeholder: t('placeholder.email'),
                input_html: { autocomplete: "email" }%>
    <%= f.input :password,
                required: true,
                placeholder: t('placeholder.password'),
                hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                input_html: { autocomplete: "new-password" } %>
    <%= f.input :password_confirmation,
                required: true,
                placeholder: t('placeholder.password_confirmation'),
                input_html: { autocomplete: "new-password" } %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Sign up", class: 'w-full' %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
